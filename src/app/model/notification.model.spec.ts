import { Notification

.
Model;
}
from;
'./notification.model';

describe('Notification.Model', () => {
    it('should create an instance', () => {
        expect(new Notification.Model()).toBeTruthy();
    });
});
